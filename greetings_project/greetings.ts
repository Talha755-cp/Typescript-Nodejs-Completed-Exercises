// greetings.ts

// Define an array of names
const names: string[] = ["John", "Jane", "Doe", "Alice"];

// Print a personalized message to each person in the array
names.forEach(name => {
    console.log(`Hello ${name}, welcome to the greetings program!`);
});
